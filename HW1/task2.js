/*
В двух переменных хранятся строки символов. 
Написать программу, которая выведет в консоль суммарное кол-во символов в обоих строках.
*/

export function symbolCounts(a,b) {
    console.log(a.length + b.length);
}